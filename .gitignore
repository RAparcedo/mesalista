# Dependencies and build output
node_modules/
dist/

# Environment variables — never commit credentials
.env

# Generated Prisma client (rebuilt by `prisma generate` on install)
server/src/generated/

# Local-only notes and credentials
pwd/
DECISIONS.md
